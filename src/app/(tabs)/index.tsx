import { Screen, Text } from '@/components/ui';

/** Przestrzeń feed. Stub — M6 fills it. See 03. */
export default function Feed() {
  return (
    <Screen>
      <Text variant="title">Przestrzeń</Text>
      <Text variant="bodySmall" tone="muted">
        Feed — makieta 03
      </Text>
    </Screen>
  );
}
